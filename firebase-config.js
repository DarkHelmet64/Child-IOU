// Fill this in with your own Firebase project's web config.
// Get it from: Firebase Console -> Project settings -> General -> "Your apps" -> Web app -> SDK setup and configuration.
// This config is safe to be public in a static site; access to your data is controlled by Firestore security rules
// (see firestore.rules), not by keeping this file secret.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
